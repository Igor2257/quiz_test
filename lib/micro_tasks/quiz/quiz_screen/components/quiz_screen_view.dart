part of '../quiz_screen.dart';
class QuizScreenView extends StatelessWidget {
  const QuizScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
